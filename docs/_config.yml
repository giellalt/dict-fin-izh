theme: jekyll-theme-giellalt
title: Dictionary for Finnish ⇒ Ingrian
description: An electronic dictionary from Finnish to Ingrian

defaults:
  - scope: { path: "" }
    values:
      layout: default

markdown: kramdown
kramdown:
  syntax_highlighter_opts:
    disable: true
